import { FC } from "react";

type TUserAboutPageProps = object;

const UserAboutPage: FC<TUserAboutPageProps> = () => {
  return (
    <div>
      <h2>This is a UserAboutPage component</h2>
    </div>
  );
};

export default UserAboutPage;
