import { FC } from "react";

type TRegisterPageProps = object;

const RegisterPage: FC<TRegisterPageProps> = () => {
  return (
    <div>
      <h2>This is a RegisterPage component</h2>
    </div>
  );
};

export default RegisterPage;
